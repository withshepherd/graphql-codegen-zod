overwrite: true
schema: './result.graphql'
generates:
  schemas.ts:
    plugins:
      - ./dist/index.js:
          onlyWithValidation: false
          zodSchemasMap:
            EmailAddress: z.string().email()
            DateTime: z.string()
            JSON: z.object()
            Date: z.string()
          zodTypesMap:
            EmailAddress: string
            TestInput: any
