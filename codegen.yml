overwrite: true
schema: './result.graphql'
generates:
  schemas.ts:
    plugins:
      - ./dist/index.js:
          onlyWithValidation: false
          zodTypesMap:
            EmailAddress: string().email()
            DateTime: string()
            JSON: object()
            Date: string()
